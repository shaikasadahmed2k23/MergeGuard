name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff

      - name: Lint with ruff
        working-directory: backend
        run: ruff check . --exit-zero

      - name: Run tests
        working-directory: backend
        env:
          GROQ_API_KEY: dummy
          GITHUB_TOKEN: dummy
          GITHUB_WEBHOOK_SECRET: testsecret
          SUPABASE_URL: https://dummy.supabase.co
          SUPABASE_KEY: dummy_key
          GITHUB_OAUTH_CLIENT_ID: dummy_client
          GITHUB_OAUTH_CLIENT_SECRET: dummy_secret
          JWT_SECRET: testjwtsecretwithatleast32byteslength
          ENCRYPTION_KEY: xYP8hsIRL6hwzC3t6iAhs2vvneVusrZfe5DlaVDo1NU=
          GOOGLE_API_KEY: dummy_google_key
        run: pytest tests/ -v
